Project: Books and Brews

Goal:
Build a real client authentication system so clients log in securely with email + password and access their own dashboard/portal.

Important:
If a file needs edits, regenerate the entire file instead of returning partial patches.

Current system already has:
- admin CRM
- client portal
- invoices
- Stripe payment flow
- package + deposit system
- file uploads via R2
- messaging
- project tracker
- project completion flow

We now need to move from lead-based portal access to true client account authentication.

Build the system cleanly for Cloudflare Pages Functions + D1.

==================================================
PHASE GOAL
==================================================

Create a secure client auth system with:

1. client accounts table
2. login page
3. logout
4. password-based session auth
5. protected client dashboard / portal
6. admin ability to create client account for a lead
7. admin ability to resend invite / set initial password flow
8. compatibility with existing portal data

==================================================
DATABASE REQUIREMENTS
==================================================

Add any required D1 SQL and return it first.

Need a client accounts table with fields similar to:

- id
- lead_id
- email
- password_hash
- created_at
- updated_at

Optional but recommended:
- last_login_at
- is_active
- invite_sent_at

If current system already has a client account table, inspect and reuse it rather than creating a conflicting structure.

Also ensure relationship between lead and client account is clear and one-to-one unless already modeled differently.

==================================================
AUTH REQUIREMENTS
==================================================

1. Secure client login page
Create or update:
- `/client/login.html`

Fields:
- email
- password

Behavior:
- POST to login route
- if valid, create session cookie
- redirect to `/client/portal.html`

2. Login API
Create or update:
- `/functions/api/client/login.js`

Requirements:
- verify email + password against stored hash
- use Web Crypto compatible hashing for Cloudflare
- create secure cookie session
- return JSON response

3. Logout API
Create or update:
- `/functions/api/client/logout.js`

Requirements:
- clear client auth cookies
- return JSON success

4. Protected client routes
Update middleware so:
- `/client/portal.html` requires client auth
- `/api/client/*` requires client auth where appropriate
- admin routes remain admin-protected
- existing file routes continue to work correctly

If middleware must change, regenerate full `functions/_middleware.js`

5. Session model
Use cookie-based auth already compatible with your existing setup style.

Cookies should be:
- HttpOnly where appropriate
- Secure
- SameSite=Lax
- Path=/

Do not use Node-only libraries.

==================================================
ADMIN REQUIREMENTS
==================================================

Admin needs to be able to create client access for a lead.

Create or update routes like:
- `/functions/api/client/create-account.js`
- `/functions/api/client/send-invite.js`

Requirements:
1. Admin can create a client account for a lead
2. Initial password can be set manually or generated
3. Optionally send invite email
4. Prevent duplicate accounts for the same lead/email unless intentional and handled cleanly

Admin UI:
Update admin pages as needed so admin can:
- create client account
- see whether account exists
- resend invite

If `admin/manage.html` or another admin page needs changes, regenerate the full file.

==================================================
PORTAL REQUIREMENTS
==================================================

Update `/client/portal.html` so it works as a true authenticated client dashboard.

Requirements:
- no dependency on public/open access
- load data for the logged-in client only
- keep all current features working:
  - project info
  - invoices
  - files
  - notes
  - reminders
  - messages
  - reply form
  - password change
  - completion/handoff view

If `client/portal.html` needs edits, regenerate the full file.

==================================================
PASSWORD MANAGEMENT
==================================================

Create or keep password change support for logged-in clients.

Requirements:
- current password required
- new password required
- update hash in DB
- success/error handling

If route exists and needs changes, regenerate full file.

==================================================
EMAIL REQUIREMENTS
==================================================

Use existing Resend setup if already present.

Create invite email flow:
- subject like: "Your Books and Brews client portal access"
- include login URL
- include temporary password or setup instructions depending on implementation

Do not break existing invoice or completion emails.

==================================================
FILES TO CHECK / POSSIBLY REGENERATE
==================================================

Inspect and update as needed. Regenerate full files where changed.

Likely files:
- `functions/_middleware.js`
- `functions/api/client/login.js`
- `functions/api/client/logout.js`
- `functions/api/client/create-account.js`
- `functions/api/client/send-invite.js`
- `functions/api/client/change-password.js`
- `functions/api/client/me.js`
- `client/login.html`
- `client/portal.html`
- `admin/manage.html`
- any admin client access page if used
- any helper auth routes already in repo

==================================================
SECURITY REQUIREMENTS
==================================================

- Cloudflare Pages Functions compatible only
- no Node-only modules
- no bcrypt if it requires unsupported runtime behavior
- use Web Crypto compatible hashing approach
- validate input carefully
- reject invalid login cleanly
- do not expose sensitive data in JSON responses

==================================================
OUTPUT FORMAT REQUIRED
==================================================

Return in this exact structure:

1. Short implementation summary
2. SQL migration statements
3. Full contents of each new or changed file
4. Notes / assumptions
5. Testing checklist

==================================================
TESTING CHECKLIST MUST COVER
==================================================

- admin creates client account
- invite email sent
- client logs in successfully
- wrong password rejected
- client can access portal only when logged in
- logout works
- password change works
- portal still loads correct invoices/files/messages
- admin routes still work
- middleware does not break uploads/files

Build the full client auth system now.