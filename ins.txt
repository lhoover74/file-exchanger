You are working in the ClearEdge Auto Glass website repository.

The site redesign and updated customer flow have already been implemented and manually tested. Your task now is a production-safe privacy/security hardening pass.

==================================================
PRIMARY GOAL
==================================================

Lock down public quote lookup so customer-facing endpoints return only payment-safe, status-safe fields.

Do not expose private customer or internal admin data through public quote lookup.

Keep the live workflow working exactly as it does now.

==================================================
CURRENT LIVE WORKFLOW TO PRESERVE
==================================================

1. Customer selects a service date online
2. Customer submits the service request
3. Request submission unlocks payment
4. Customer can complete payment immediately
5. After successful payment, the appointment remains tied to the date selected by the customer
6. Admin can still review, manage, override, and update the job internally

Do not break this flow.

==================================================
WHAT MUST BE FIXED
==================================================

Right now, public quote lookup is too permissive.

Customer-facing/public quote lookup must NOT expose:
- phone
- email
- address
- internal notes
- schedule notes
- admin-only fields
- payment provider secrets or internal IDs
- any sensitive customer or staff-only data

Public quote lookup should return only the minimum fields needed for:
- payment page
- status page
- customer-facing confirmation flow

==================================================
PUBLIC RESPONSE SHOULD ONLY INCLUDE SAFE FIELDS
==================================================

Only return fields like these from public quote lookup, if they are needed:

- id
- status
- serviceType
- year
- make
- model
- approvedAmount or final charge amount if needed for payment display
- calculatedAmount only if truly needed
- scheduleDate
- scheduleTime
- scheduleWindow
- fulfillmentStatus
- paymentProvider (only if safe and useful to customer)
- payment state / payment status fields that are safe for display
- any safe customer-facing appointment status text if needed

Do NOT return:
- firstName / lastName unless absolutely necessary and clearly justified
- phone
- email
- service address
- VIN
- damage description
- uploaded photo data
- admin notes
- scheduleNotes
- webhook metadata
- Stripe session IDs unless strictly needed by a secure status endpoint
- OpenNode internal charge details beyond what is safe for customer-facing status
- internal timestamps unless needed and safe

Default to least privilege.

==================================================
FILES TO INSPECT AND UPDATE
==================================================

Inspect and update the live files that actually serve this flow, including at minimum:
- _worker.js
- [[path]].js
- payment.html
- status.html
- payment-success.html
- control-center-7h9.html only if admin/public endpoint separation needs clarification

If only one backend file is actually active, still inspect both before changing logic.

==================================================
IMPLEMENTATION RULES
==================================================

1. Keep admin endpoints unchanged in capability
- Admin must still be able to access full quote data where needed
- Admin token based flows must keep working

2. Separate public-safe and admin/full responses clearly
- Public endpoints should return sanitized quote objects
- Admin endpoints may keep full objects if already protected

3. Do not break:
- quote submission
- payment lookup
- status lookup
- Stripe checkout
- OpenNode/Bitcoin checkout if configured
- payment success status verification
- admin quote management
- quote ID based lookup flow

4. Do not redesign the site
- This is a privacy/security hardening pass only
- Minimal UI changes only if required to support the reduced public response

5. Do not rename routes unless absolutely necessary
6. Do not remove backward compatibility unless clearly safe
7. Keep implementation simple and production-safe

==================================================
RECOMMENDED APPROACH
==================================================

- Identify all public quote lookup responses
- Add a sanitization layer for public/customer-facing quote data
- Ensure admin-authenticated endpoints still return full records where needed
- Update payment.html and status.html so they only depend on safe public fields
- Ensure payment-success.html still works with status verification endpoints
- Preserve any provider verification logic that must remain secure

If useful, create a small helper function such as:
- sanitizePublicQuote(quote)

Use it consistently across public endpoints.

==================================================
VALIDATION REQUIREMENTS
==================================================

Before finishing, verify:

1. A public quote lookup still works for:
- payment.html
- status.html
- payment-success.html status checks if applicable

2. Public quote lookup no longer exposes:
- phone
- email
- address
- notes
- VIN
- internal metadata

3. Admin flows still work:
- load quotes
- view quote details
- set pricing
- update status
- run payment
- mark paid / completed / canceled

4. No route names are broken
5. No customer-facing flow is broken

==================================================
DELIVERABLES
==================================================

Make the changes now and return:

1. files changed
2. what public fields were removed from customer-facing responses
3. what safe fields remain exposed publicly
4. confirmation that admin endpoints still return full data
5. any remaining privacy/security risks